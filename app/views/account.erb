<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <%if Helpers.is_logged_in?(session)%>
      <h1>Username: <%=Helpers.current_user(session).username%></h1>
      <h2>Current Balance: $<%=Helpers.current_user(session).balance%></h2>
    <%else%>
      <a href="/">Home</a>
    <%end%>


    <a href="/logout"><button>Log Out</button></a>
  </body>
</html>
